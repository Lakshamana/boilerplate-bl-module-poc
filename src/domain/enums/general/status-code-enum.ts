export enum StatusCodeEnum {
  SUCCESS = 200,
  UNAUTHORIZED = 401,
  BAD_REQUEST = 400,
  TIMEOUT = 408,
  SERVER_ERROR = 500,
  BAD_GATEWAY = 502,
  UNAVAILABLE = 503
}

export enum HttpStatusCode {
  success = 200,
  created = 201,
  noContent = 204,
  badRequest = 400,
  unauthorized = 401,
  forbidden = 403,
  notFound = 404,
  timeout = 408,
  serverError = 500,
  badGateway = 502,
  unprocessable = 422
}
